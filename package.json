{
  "name": "flux-demo",
  "version": "0.4.0",
  "description": "HuggingFace endpoint demo",
  "keywords": [
    "HuggingFace",
    "flux"
  ],
  "repository": {
    "type": "git",
    "url": "github.com/jacksmedia/template"
  },
  "license": "MIT",
  "author": "jacksmedia",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "npm run test",
    "build": "cd webapp/client && npm install && npm run build && cd ../server && npm install",
    "dev": "concurrently \"cd ./webapp/server && npm run dev\" \"cd ./webapp/client && npm run dev\"",
    "start": "cd webapp/server && node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0"
  }
}
