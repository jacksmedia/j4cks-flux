{
  "name": "flux-demo",
  "version": "0.3.0",
  "description": "HuggingFace Gen AI demo",
  "keywords": [
    "HuggingFace",
    "flux"
  ],
  "repository": {
    "type": "git",
    "url": "github.com/jacksmedia/template"
  },
  "license": "MIT",
  "author": "jacksmedia",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"No tests specified\" && exit 0",
    "install:all": "npm install && cd webapp/server && npm install && cd ../client && npm install",
    "build:client": "cd webapp/client && npm run build",
    "dev": "concurrently \"cd webapp/server && npm run dev\" \"cd webapp/client && npm run dev\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
